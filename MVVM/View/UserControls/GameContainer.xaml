<UserControl x:Class="TurboTyper.MVVM.View.UserControls.GameContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TurboTyper.MVVM.View.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="620" d:DesignWidth="1920">
    <Grid Margin="40 50">
        <!-- display lines -->
        <ItemsControl ItemsSource="{Binding Game.GameText}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <!-- display chars -->
                    <ItemsControl ItemsSource="{Binding Text}" HorizontalAlignment="Left">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                
                                <TextBlock Text="{Binding Converter={StaticResource ObservableCharToStringConverter}}"
                                           Style="{StaticResource CharStyle}"/>
                                
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
